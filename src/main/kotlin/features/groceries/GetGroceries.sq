getApartmentIdByUserId:
SELECT apartment_id
FROM users
WHERE id = :userId;

getGroceriesFromApartment:
SELECT g.id, g.name, creator.username AS addedByUsername
FROM groceries g
JOIN users creator ON g.creator_id = creator.id
WHERE g.bought_at IS NULL AND g.apartment_id = :apartmentId;