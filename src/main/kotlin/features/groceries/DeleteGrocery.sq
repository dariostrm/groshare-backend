deleteGrocery:
DELETE FROM groceries
WHERE id = :id;

getGrocerysApartmentByID:
SELECT apartment_id
FROM groceries
WHERE bought_at IS NULL AND id = :id;

getApartmentIdByUserId:
SELECT apartment_id
FROM users
WHERE id = :userId;